# Dependencies
node_modules/
.pnp
.pnp.js

# Environment variables
.env
.env.local
.env.production
.env.*.local

# Build outputs
.next/
out/
build/
dist/

# Sample PDFs (keep only small fixtures under public/samples)
*.pdf
!public/samples/*.pdf

# Testing & logs
coverage/
*.log

# TypeScript build cache
*.tsbuildinfo

# OS / editor
.DS_Store
Thumbs.db

# Local Netlify folder
.netlify

# /brag-generated promo video project (not part of the app; large, and
# ClauseLens repo has a strict <10MB submission cap)
brag-output/

# Automated demo-video recording project (Playwright + Kokoro TTS) — not
# part of the app, not committed
demo-video/

# Merged submission video (brag + walkthrough) — not part of the app,
# well over what the repo needs to carry
clauselens-full-submission-video.mp4
